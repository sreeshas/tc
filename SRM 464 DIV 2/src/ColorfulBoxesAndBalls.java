import java.util.*;
import java.math.*;
import static java.lang.Math.*;

public class ColorfulBoxesAndBalls {
	
	public int getMaximum(int numRed, int numBlue, int onlyRed, int onlyBlue, int bothColors) {
        int[] redboxes = new int[numRed];
		int[] blueboxes = new int[numBlue];
		int redballs = numRed;
		int blueballs= numBlue;
        //fill redballs
		int redballcount = redballs;
		int blueballcount;
		int result = 0;
		while(redballcount >0) {
			if( blue)
			result = result+ max(onlyRed, bothColors);
		}

 		return 0;
	}
}
